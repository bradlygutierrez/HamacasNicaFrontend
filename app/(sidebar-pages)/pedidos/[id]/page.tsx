"use client";
import { useParams, useRouter } from "next/navigation";
import { useCallback, useEffect, useState } from "react";
import { apiFetch } from "@/app/_lib/api";
import { useCatalogCapabilities } from "@/app/_components/catalog-permissions-provider";
type Material={id:number;nombre:string;cantidad_requerida:string;cantidad_compra_plan:number;unidad_compra:string;estado:string;cantidad_compra_real:string;precio_compra_snapshot?:string;costo_consumo_estimado?:string;costo_compra_estimado?:string;costo_compra_real?:string};
type Process={id:number;nombre:string;orden:number|null;estado:string;costo_estimado?:string};
type Pedido={id:number;numero:string;estado:string;proforma_numero:string|null;nombre_cliente:string;direccion?:string;telefono?:string;fecha_pedido:string;fecha_entrega_estimada:string|null;fecha_terminado:string|null;detalles:Array<{nombre:string;cantidad:number;precio_unitario:string;descuento:string;subtotal:string;servicios:Array<{nombre:string;cantidad:string;precio_unitario:string;subtotal:string}>}>;servicios_pedido:Array<{nombre:string;cantidad:string;precio_unitario:string;subtotal:string}>;materiales:Material[];procesos:Process[];progreso:{materiales_total:number;materiales_listos:number;procesos_total:number;procesos_completados:number};historial:Array<{estado_anterior:string|null;estado_nuevo:string;comentario?:string}>;analisis_interno?:Record<string,string|number>};
export default function PedidoDetailPage(){
 const {id}=useParams<{id:string}>();const router=useRouter();const {canEdit}=useCatalogCapabilities("/pedidos");const [role,setRole]=useState("");const [pedido,setPedido]=useState<Pedido|null>(null);const [error,setError]=useState("");
 const load=useCallback(async()=>{const response=await apiFetch("/pedidos/"+id);const data=await response.json().catch(()=>null);if(!response.ok){setError(data?.message??"No se pudo cargar el pedido.");return;}setPedido(data.data)},[id]);
 useEffect(()=>{void(async()=>{await load();const response=await apiFetch("/me");const data=await response.json().catch(()=>null);setRole(data?.data?.rol??"")})()},[load]);
 async function status(next:string,comment?:string){const response=await apiFetch("/pedidos/"+id+"/estado",{method:"POST",body:JSON.stringify({estado:next,comentario:comment})});const data=await response.json().catch(()=>null);if(!response.ok){setError(data?.message??"No se pudo cambiar el estado.");return;}setPedido(data.data)}
 async function material(materialId:number,changes:Record<string,unknown>){const response=await apiFetch("/pedidos/"+id+"/materiales/"+materialId,{method:"PUT",body:JSON.stringify(changes)});const data=await response.json().catch(()=>null);if(!response.ok){setError(data?.message??"No se pudo actualizar el material.");return;}await load()}
 async function process(processId:number,next:string){const response=await apiFetch("/pedidos/"+id+"/procesos/"+processId,{method:"PUT",body:JSON.stringify({estado:next})});const data=await response.json().catch(()=>null);if(!response.ok)setError(data?.message??"No se pudo actualizar el proceso.");else await load()}
 if(!pedido)return <div className="p-6">{error||"Cargando..."}</div>;const operate=canEdit&&["admin","almacenista"].includes(role);const materialsReady=pedido.progreso.materiales_total===pedido.progreso.materiales_listos;const processesDone=pedido.progreso.procesos_total===pedido.progreso.procesos_completados;
 return <div className="w-full bg-[#456f89] px-3 py-4 text-[#08264d] sm:px-8 sm:py-7"><header className="mb-5 flex flex-wrap items-center justify-between gap-3 text-white"><div><h1 className="text-3xl font-extrabold sm:text-5xl">{pedido.numero}</h1><p className="capitalize">{pedido.estado.replaceAll("_"," ")}</p></div><button onClick={()=>router.push("/pedidos")} className="rounded bg-white px-3 py-2 text-sm font-bold">Volver</button></header>{error?<p className="mb-4 rounded bg-red-50 p-3 text-sm text-red-700">{error}</p>:null}<div className="grid gap-5 xl:grid-cols-[minmax(0,1fr)_360px]"><main className="space-y-5"><section className="rounded bg-[#e9eef1] p-5 shadow-lg"><h2 className="text-xl font-extrabold">Pedido y cliente</h2><p>{pedido.nombre_cliente}</p><p>{pedido.direccion} · {pedido.telefono}</p><p>Proforma: {pedido.proforma_numero??"—"} · Entrega: {pedido.fecha_entrega_estimada??"—"}</p></section><section className="rounded bg-[#e9eef1] p-5 shadow-lg"><h2 className="text-xl font-extrabold">Productos</h2>{pedido.detalles.map((line,index)=><div key={index} className="mt-3 rounded bg-white p-3"><p className="font-bold">{line.nombre}</p><p>{line.cantidad} × C$ {line.precio_unitario} · Descuento C$ {line.descuento} · Subtotal C$ {line.subtotal}</p>{line.servicios.map((service,serviceIndex)=><p className="text-sm" key={serviceIndex}>{service.nombre}: {service.cantidad} × C$ {service.precio_unitario} · C$ {service.subtotal}</p>)}</div>)}<h3 className="mt-4 font-bold">Servicios generales</h3>{pedido.servicios_pedido.map((service,index)=><p key={index}>{service.nombre}: {service.cantidad} × C$ {service.precio_unitario} · C$ {service.subtotal}</p>)}</section><section className="rounded bg-[#e9eef1] p-5 shadow-lg"><h2 className="text-xl font-extrabold">Materiales</h2>{pedido.materiales.map(item=><div key={item.id} className="mt-2 flex flex-wrap items-center justify-between gap-2 rounded bg-white p-3"><span>{item.nombre}: {item.cantidad_requerida} · {item.cantidad_compra_plan} {item.unidad_compra}</span><span className="capitalize">{item.estado}</span>{operate?<button onClick={()=>void material(item.id,{estado:item.estado==="pendiente"?"parcial":"listo"})} className="rounded bg-[#456f89] px-2 py-1 text-xs text-white">Actualizar</button>:null}</div>)}</section><section className="rounded bg-[#e9eef1] p-5 shadow-lg"><h2 className="text-xl font-extrabold">Procesos</h2>{pedido.procesos.map(item=><div key={item.id} className="mt-2 flex items-center justify-between rounded bg-white p-3"><span>{item.orden??"—"} · {item.nombre}</span><span className="capitalize">{item.estado}</span>{operate?<button onClick={()=>void process(item.id,item.estado==="pendiente"?"en_proceso":"completado")} className="rounded bg-[#456f89] px-2 py-1 text-xs text-white">Actualizar</button>:null}</div>)}</section><section className="rounded bg-[#e9eef1] p-5 shadow-lg"><h2 className="text-xl font-extrabold">Historial</h2>{pedido.historial.map((item,index)=><p key={index} className="text-sm">{item.estado_anterior??"—"} → {item.estado_nuevo} · {item.comentario??""}</p>)}</section></main><aside className="rounded bg-[#e9eef1] p-5 shadow-lg"><h2 className="text-xl font-extrabold">Progreso</h2><p>Materiales: {pedido.progreso.materiales_listos}/{pedido.progreso.materiales_total}</p><p>Producción: {pedido.progreso.procesos_completados}/{pedido.progreso.procesos_total}</p>{operate?<div className="mt-4 space-y-2">{pedido.estado==="pendiente"?<button onClick={()=>void status(materialsReady?"materiales_listos":"materiales_pendientes")} className="w-full rounded bg-[#123852] px-3 py-2 text-sm font-bold text-white">Iniciar preparación</button>:null}{pedido.estado==="materiales_pendientes"&&materialsReady?<button onClick={()=>void status("materiales_listos")} className="w-full rounded bg-[#123852] px-3 py-2 text-sm font-bold text-white">Marcar materiales listos</button>:null}{pedido.estado==="materiales_listos"?<button onClick={()=>void status("en_produccion")} className="w-full rounded bg-[#123852] px-3 py-2 text-sm font-bold text-white">Iniciar producción</button>:null}{pedido.estado==="en_produccion"&&processesDone?<button onClick={()=>void status("terminado")} className="w-full rounded bg-[#123852] px-3 py-2 text-sm font-bold text-white">Terminar pedido</button>:null}{role==="admin"&&!["terminado","cancelado"].includes(pedido.estado)?<button onClick={()=>{const reason=window.prompt("Motivo de cancelación");if(reason)void status("cancelado",reason)}} className="w-full rounded bg-red-700 px-3 py-2 text-sm font-bold text-white">Cancelar pedido</button>:null}</div>:null}{pedido.estado==="terminado"?<p className="mt-4 rounded bg-white p-3 text-sm">Pendiente de facturación</p>:null}{pedido.analisis_interno?<div className="mt-4 border-t pt-3 text-sm"><h3 className="font-bold">Análisis interno</h3><p>Costo producción: C$ {pedido.analisis_interno.costo_total_estimado}</p><p>Compra estimada: C$ {pedido.analisis_interno.costo_compra_estimado}</p><p>Compra real: C$ {pedido.analisis_interno.costo_compra_real_total}</p><p>Utilidad: C$ {pedido.analisis_interno.utilidad_estimada}</p></div>:null}</aside></div></div>;
}
